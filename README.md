# wigglegraph


```commandline
    pip install pre-commit
    pre-commit install # Sets up the git hooks script
    pre-commit run --all-files # https://pre-commit.com/#4-optional-run-against-all-the-files
```
